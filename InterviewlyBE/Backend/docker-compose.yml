# Local development services for InterviewlyAI Backend
#
# Start:  docker-compose up -d
# Stop:   docker-compose down

version: '3.8'

services:
  redis:
    image: redis:latest
    container_name: interviewly-redis
    ports:
      - "6379:6379"
    restart: unless-stopped
